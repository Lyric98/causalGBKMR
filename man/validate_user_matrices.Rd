% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01-validation.R
\name{validate_user_matrices}
\alias{validate_user_matrices}
\title{Input validation utilities for g-BKMR}
\usage{
validate_user_matrices(Y, Z, X, T, Adim, Ldim, n_baseline)
}
\arguments{
\item{Y}{Numeric vector. Outcome variable (length n).}

\item{Z}{Numeric matrix. Mixture exposure matrix (n x (Adim x T)).}

\item{X}{Numeric matrix. Covariate matrix (n x (Ldim x T + baseline_covs)).}

\item{T}{Integer. Number of time points.}

\item{Adim}{Integer. Number of mixture components per time point.}

\item{Ldim}{Integer. Number of time-dependent covariates per time point.}

\item{n_baseline}{Integer. Number of baseline covariates.}
}
\value{
Invisible TRUE if validation passes, stops with error if validation fails.
}
\description{
Contains functions to validate user inputs before g-BKMR analysis.
These functions ensure data quality and parameter consistency.
Validate user-provided matrices for g-BKMR analysis

Performs comprehensive validation of input matrices Y, Z, X
to ensure they meet the requirements for g-BKMR analysis.
}
\details{
The function validates:
\itemize{
\item Data types and dimensions
\item Missing values (warnings)
\item Parameter consistency
\item Matrix dimension compatibility
}
}
\examples{
\dontrun{
n <- 100
Y <- rnorm(n)
Z <- matrix(rnorm(n * 6), nrow = n, ncol = 6)  # 2 metals x 3 time points
X <- matrix(rnorm(n * 5), nrow = n, ncol = 5)  # 1 TD cov x 3 time points + 2 baseline

validate_user_matrices(Y, Z, X, T = 3, Adim = 2, Ldim = 1, n_baseline = 2)
}

}
\keyword{internal}
